# Configuration for Scott's computer

To install NixOS configuration, run:

```sh
nixos-rebuild switch --flake .
```

To install home-manager configuration, run:

```sh
home-manager switch --flake .
```

